import React from 'react';
import './Offer.css';

const Offer = (props) => {
    const {pairImage,name,price} = props.allOffers;
    
    
    return (
        <div className="offer-wrapper">
            {
                
            }
        </div>
    );
};

export default Offer;